# 使用说明

本工具是 **Windows 专用、只读、需授权** 的 Wi-Fi 安全自检工具，仅用于自有网络或已取得书面授权的实验室环境。

## 核心能力

1. 只读扫描 SSID、BSSID、信号、信道与加密类型。
2. 通过 `netsh wlan show profiles/profile` 检查已保存配置元数据，不显示、不保存明文密码。
3. 对开放网络、WEP、旧版 WPA 等情况给出风险评级。
4. 记录授权、扫描和配置检查事件，JSONL 日志采用 SHA-256 哈希链防篡改。
5. 将结果导出为 CSV；安装 reportlab 后可扩展 PDF。

## 操作步骤

1. 在 Windows 上运行 `python wifi_cracker_optimized.py`。
2. 填写授权人、组织、用途及授权凭证/工单引用，点击“确认授权”。
3. 点击“只读扫描”查看附近热点元数据。
4. 点击“检查已保存配置”查看配置的认证/加密元数据。
5. 点击“导出 CSV”保存审计报告。

## 明确禁止

工具不会尝试连接任何热点，不接受密码或密码字典，不写入 Wi-Fi 配置，不执行连接/断开操作，也不会读取 `key=clear`。

## 故障排查

- 非 Windows 系统：程序会提示仅支持 Windows 并退出。
- 扫描失败：确认无线网卡启用，并允许执行只读 `netsh wlan show` 命令。
- 无数据可导出：先完成扫描或保存配置检查。
