# WiFi密码破解工具使用说明

## 一、功能简介

这是一个基于 Python 的 WiFi 密码破解工具，使用图形化界面（GUI）进行操作。主要功能包括：

- **扫描附近WiFi**：自动搜索周围的WiFi热点
- **显示WiFi信息**：展示WiFi名称(SSID)、物理地址(BSSID)、信号强度(signal)
- **字典暴力破解**：通过密码字典文件逐个尝试连接
- **自动验证密码**：自动尝试连接并验证密码是否正确

## 二、环境要求

### 1. 必备软件
- Python 3.x
- Windows 操作系统（需要无线网卡）

### 2. 依赖库
需要安装以下Python库：

```bash
pip install pywifi
```

`tkinter` 库通常随Python安装自带，无需单独安装。

## 三、文件说明

| 文件名 | 说明 |
|--------|------|
| `pywifi.py` | 主程序文件，运行此文件启动工具 |
| `wifi密码本.txt` | 密码字典文件，包含待尝试的密码列表 |

## 四、使用方法

### 1. 启动程序

```bash
python pywifi.py
```

程序启动后会显示一个图形界面。

### 2. 界面说明

```
┌─────────────────────────────────────┐
│              配置                    │
├─────────────────────────────────────┤
│ [搜索附近WiFi]  [开始破解]            │
├─────────────────────────────────────┤
│ 目录路径: [________] [添加密码文件目录]│
├─────────────────────────────────────┤
│ WiFi账号: [________] WiFi密码: [____]│
├─────────────────────────────────────┤
│            WiFi列表                  │
│ ┌─────────────────────────────────┐ │
│ │ WiFiID│  SSID  │ BSSID  │signal │ │
│ ├─────────────────────────────────┤ │
│ │   1   │ xxxxxx │ xxxxxx │  -50  │ │
│ │   2   │ xxxxxx │ xxxxxx │  -65  │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 3. 操作步骤

1. **选择密码字典**
   - 点击「添加密码文件目录」按钮
   - 选择包含密码列表的文本文件（如 `wifi密码本.txt`）
   - 文件路径会显示在「目录路径」框中

2. **扫描WiFi**
   - 点击「搜索附近WiFi」按钮
   - 等待约15秒，程序会扫描周围WiFi
   - 扫描结果会显示在下方的WiFi列表中

3. **选择目标WiFi**
   - 在WiFi列表中**双击**要破解的WiFi
   - WiFi名称会自动填入「WiFi账号」框中

4. **开始破解**
   - 点击「开始破解」按钮
   - 程序会逐个尝试密码字典中的密码
   - 破解成功后会弹出提示框显示正确密码

## 五、密码字典格式

密码字典文件应为纯文本文件，每行一个密码：

```
12345678
88888888
admin123
password
11111111
...
```

## 六、注意事项

1. **网卡要求**：电脑必须配备无线网卡，且网卡支持 monitor 模式
2. **破解时间**：取决于密码字典大小和网络响应速度
3. **成功率**：仅当目标WiFi密码在字典中时才可能成功
4. **合法性**：仅限用于自己拥有所有权的WiFi网络测试

## 七、技术原理

1. 使用 `pywifi` 库调用系统无线网卡接口
2. 扫描获取周围WiFi的 SSID 和 BSSID
3. 创建WiFi配置文件，设置连接参数：
   - 认证方式：开放认证
   - 加密算法：WPA2-PSK
   - 加密单元：CCMP
4. 逐个尝试密码，通过连接状态判断是否成功

## 八、常见问题

| 问题 | 解决方法 |
|------|----------|
| 无法扫描到WiFi | 检查无线网卡是否启用 |
| 破解总是失败 | 确认密码字典中包含正确密码 |
| 程序报错 | 检查是否安装了 `pywifi` 库 |
| 连接超时 | 适当调整代码中的 `time.sleep()` 时间 |

## 九、代码结构

```
MY_GUI 类
├── __init__()          # 初始化网卡和变量
├── set_init_window()   # 创建GUI界面
├── scans_wifi_list()   # 扫描WiFi
├── show_scans_wifi_list()  # 显示WiFi列表
├── add_mm_file()       # 选择密码文件
├── onDBClick()         # 双击选择WiFi
├── readPassWord()      # 读取字典并破解
└── connect()           # 连接验证密码
```
