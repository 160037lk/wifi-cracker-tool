[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "authorized-wifi-auditor"
version = "1.0.0"
description = "Windows read-only authorized Wi-Fi security auditor"
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
